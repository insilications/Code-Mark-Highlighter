{
  "name": "code-mark-highlighter",
  "displayName": "Code Mark Highlighter",
  "version": "0.4.0",
  "description": "Highlight code blocks with custom colors and tags. Highlights persist based on code content — not line numbers — and survive edits and restarts.",
  "categories": [
    "Other",
    "Programming Languages"
  ],
  "keywords": [
    "bookmark",
    "code",
    "code mark",
    "highlight",
    "navigator",
    "tags"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/insilications/Code-Mark-Highlighter"
  },
  "publisher": "insilications",
  "type": "module",
  "main": "./out/extension.js",
  "scripts": {
    "build": "bun run clean && bun run build:extension && bun run build:webview",
    "build:extension": "vite build --mode extension",
    "build:webview": "vite build --mode webview",
    "check": "tsc --build --clean && tsc --build --emitDeclarationOnly",
    "clean": "rm -rf out",
    "fmt": "oxfmt --write",
    "fmt:check": "oxfmt --check",
    "lint": "bun run lint:biome && bun run lint:oxlint",
    "lint:biome": "biome check --reporter=concise --max-diagnostics 999",
    "lint:biome:fix": "biome check --write",
    "lint:biome:full": "biome check --max-diagnostics 999 --verbose",
    "lint:biome:summary": "biome check --max-diagnostics 999 --reporter=summary",
    "lint:fix": "bun run lint:biome:fix && bun run lint:oxlint:fix",
    "lint:oxlint": "oxlint --format=agent",
    "lint:oxlint:fix": "oxlint --fix",
    "lint:oxlint:full": "oxlint",
    "lint:oxlint:sarif": "oxlint --format=sarif",
    "package": "vsce package --no-dependencies",
    "vscode:prepublish": "bun run build"
  },
  "dependencies": {
    "@apidevtools/json-schema-ref-parser": "^16.0.2",
    "@vscode-elements/elements": "file:../elements",
    "@vscode/codicons": "^0.0.46-39",
    "litedent": "^0.1.0",
    "vscode-messenger": "^0.7.0",
    "vscode-messenger-common": "^0.7.0",
    "vscode-messenger-webview": "^0.7.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.12",
    "@faker-js/faker": "^10.6.0",
    "@types/bun": "^1.4.1",
    "@types/node": "^26.5.0",
    "@types/vscode": "^1.136.0",
    "dereference-json-schema": "^0.2.3",
    "eslint": "^10.10.0",
    "eslint-plugin-legibility": "^0.4.0",
    "eslint-plugin-regexp": "^3.3.0",
    "json-schema-faker": "^0.6.3",
    "oxfmt": "^0.67.0",
    "oxlint": "^1.82.0",
    "oxlint-tsgolint": "^7.0.2001",
    "rolldown": "^1.2.7",
    "typescript": "^7.1.0-dev.20260907.1",
    "vite": "^8.2.2"
  },
  "contributes": {
    "commands": [
      {
        "category": "Code Mark",
        "command": "codemark.highlightCodeQuick",
        "title": "Code Mark: Quick Highlight Code",
        "when": "codemark.isActivated"
      },
      {
        "category": "Code Mark",
        "command": "codemark.showPanel",
        "title": "Code Mark: Show Highlights Panel",
        "when": "codemark.isActivated"
      }
    ],
    "configuration": {
      "properties": {
        "codemark.fuzzyMatchThreshold": {
          "default": 0.75,
          "description": "Minimum similarity ratio (0.5 - 1.0) for fuzzy content matching when re-applying highlights.",
          "maximum": 1,
          "minimum": 0.5,
          "type": "number"
        },
        "codemark.storageFile": {
          "default": ".vscode/codemark.json",
          "description": "Path (relative to workspace root) where highlights are stored.",
          "type": "string"
        }
      },
      "title": "Code Mark Highlighter"
    },
    "views": {
      "codemark-sidebar": [
        {
          "contextualTitle": "Code Mark Highlights",
          "icon": "resources/devmarks.svg",
          "id": "codemark.highlightsPanel",
          "name": "Highlights",
          "type": "webview",
          "when": "codemark.isActivated"
        }
      ]
    },
    "viewsContainers": {
      "activitybar": [
        {
          "icon": "resources/devmarks.svg",
          "id": "codemark-sidebar",
          "title": "Code Mark Highlighter"
        }
      ]
    }
  },
  "activationEvents": [
    "onStartupFinished"
  ],
  "icon": "resources/devmarks-icon.png",
  "engines": {
    "vscode": "^1.136.0"
  },
  "trustedDependencies": [
    "core-js-bundle",
    "esbuild",
    "rs-module-lexer"
  ]
}
